""" Description:
Given a sorted array of distinct integers and a target value, return the index if the target is found. If not, return the index where it would be if it were inserted in order.
"""
nums = list(map(int,input("Enter array elements in sorted order: ").split()))
target = int(input("Enter target: "))
left=0
right=len(nums)-1
while left<=right:
    mid=(left+right)//2
    if target==nums[mid]:
        print(mid)
        break
    if target<nums[mid]:
        right=mid-1
    if target>nums[mid]:
        left=mid+1
else:
    print(left)
