arr=list(map(int,input("Enter array elements: ").split()))
j=0
for i in range(len(arr)):
    if arr[i]==0:
        j=i
        break
for i in range(i+1,len(arr)):
    if arr[i]!=0:
        arr[i],arr[j]=arr[j],arr[i]
        j+=1
print("Array after moving zeros to an end:\n",arr)
