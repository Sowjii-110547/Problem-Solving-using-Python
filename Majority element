# majority element that appears more than n/2 times where n is the length of the array.
arr=list(map(int,input("Enter array elements: ").split()))
for i in range(len(arr)):
    count=0
    for j in range(len(arr)):
        if arr[j]==arr[i]:
            count+=1
    if count>len(arr)//2:
        print(arr[i])
        break
else:
    print(-1)
